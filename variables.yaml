# AWS Credentials:
awsProfile: default      # aws configure list-profiles
awsUsername: jason.krause     # aws sts get-caller-identity --query Arn --output text --profile <YOUR_PROFILE> | cut -f 2 -d '/'

# EC2 Instance Options:
instanceType: t2.2xlarge
volumeSize: 50

# Specify Versions:
k3dVersion: v4.4.7

# Registry Credentials (for installing Flux):
registryUsername: Jason_Krause
registryPassword: 9zn8nvhwfM